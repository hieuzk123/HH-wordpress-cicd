filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/wordpress/*.log

output.elasticsearch:
  hosts: ["host.docker.internal:9200"]
  index: "wordpress-logs-%{+yyyy.MM.dd}"

setup.template.name: "wordpress"
setup.template.pattern: "wordpress-logs-*"
setup.ilm.enabled: false
